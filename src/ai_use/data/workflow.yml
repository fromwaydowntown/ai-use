name: ai-use

on:
  pull_request:
    types: [opened, synchronize, reopened, ready_for_review, closed]

permissions:
  contents: read
  checks: write

jobs:
  attribution:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Fetch attribution refs
        run: |
          git fetch origin '+refs/ai-attribution/*:refs/ai-attribution/*' || true

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install attribution CLI
        run: pip install "git+https://github.com/fromwaydowntown/ai-use.git"

      - name: Collect events from all developers
        run: |
          ai-use fetch-telemetry \
            --repo . \
            --output .ai-use/fetched-events.ndjson \
            --github-native || true

      - name: Download PR diff
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: gh pr diff ${{ github.event.pull_request.number }} > pr.diff

      - name: Post attribution check
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          ai-use analyze-pr \
            --repo . \
            --diff-file pr.diff \
            --commit-sha "${{ github.event.pull_request.head.sha }}" \
            --events-file .ai-use/fetched-events.ndjson \
            --post-check \
            ${{ github.event.pull_request.merged && '--final' || '' }}
